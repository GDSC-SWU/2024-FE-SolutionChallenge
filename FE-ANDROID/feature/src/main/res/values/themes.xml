<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.FE" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:colorPrimary">@color/main_color</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowBackground">@color/bg_white</item>
    </style>

    <style name="Theme.FE" parent="Base.Theme.FE" />
</resources>